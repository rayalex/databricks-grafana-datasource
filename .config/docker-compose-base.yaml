services:
  grafana:
    user: root
    container_name: 'rayalex-databricks-datasource'

    build:
      context: .
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-11.5.2}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
      - 2345:2345/tcp # delve
    security_opt:
      - 'apparmor:unconfined'
      - 'seccomp:unconfined'
    cap_add:
      - SYS_PTRACE
    volumes:
      - ../dist:/var/lib/grafana/plugins/rayalex-databricks-datasource
      - ../provisioning:/etc/grafana/provisioning
      - ../dashboards:/var/lib/grafana/dashboards
      - ..:/root/rayalex-databricks-datasource

    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.rayalex-databricks-datasource:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: rayalex-databricks-datasource
